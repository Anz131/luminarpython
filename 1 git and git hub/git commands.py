# clone: remote->local storage(exact copy)
# push: loca;->remote(updation)
# pull: remote->local(updation)
#
# git clone: remote->local(exact copy)
# gity add -A: ls->staging area
# git commit: staA->local repository
# git push: lr->remote repo